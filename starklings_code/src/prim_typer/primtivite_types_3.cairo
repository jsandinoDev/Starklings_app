fn main() {
    let cat = ('Furry McFurson', 3); // don't change this line
    let (name, age) = cat; // your pattern here = cat;
    println!("name is {}", name);
    println!("age is {}", age);
}


// importants here is the destructure of the tuple cat